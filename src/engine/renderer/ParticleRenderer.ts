import{Application,Graphics}from 'pixi.js';interface Particle{sprite:Graphics;x:number;y:number;speedY:number;baseRadius:number;baseX:number;angle:number;shape:string;}export class ParticleRenderer{private app:Application|null=null;private isInitialized=false;private particleGroups:Map<string,Particle[]>=new Map();public async init(container:HTMLDivElement|null,width:number,height:number){if(this.isInitialized)return;this.app=new Application();await this.app.init({width,height,backgroundAlpha:0,antialias:true});if(this.app.canvas&&container){this.app.canvas.style.position='absolute';this.app.canvas.style.top='0';this.app.canvas.style.left='0';this.app.canvas.style.pointerEvents='none';container.appendChild(this.app.canvas);}this.isInitialized=true;}public getCanvas():HTMLCanvasElement|null{return(this.app?.canvas as HTMLCanvasElement)??null;}public destroy(){if(this.app){try{this.app.destroy(true,{children:true,texture:true});}catch(e){console.warn('Gagal membersihkan instance Pixi:',e);}this.app=null;this.isInitialized=false;}}public render(data:Uint8Array,layers:any[],width:number,height:number){if(!this.isInitialized||!this.app)return;let sum=0;for(let i=0;i<10;i++)sum+=data[i];const avgBass=sum/10;const activeLayerIds=layers.map(l=>l.id);for(const[id,particles]of this.particleGroups.entries()){if(!activeLayerIds.includes(id)){particles.forEach(p=>p.sprite.destroy());this.particleGroups.delete(id);}}layers.forEach(layer=>{const config=layer.preset.config;const size=layer.size;const avg=avgBass*config.sensitivity;if(!this.particleGroups.has(layer.id))this.particleGroups.set(layer.id,[]);const particles=this.particleGroups.get(layer.id)!;const count=config.particleCount||50;while(particles.length<count){const p=new Graphics();const startX=Math.random()*width;let startY=Math.random()*height;let speed=Math.random()*1+0.5;let color=config.colors[Math.floor(Math.random()*config.colors.length)];if(config.shape==='smoke'){p.circle(0,0,Math.random()*40+40);p.fill({color:color,alpha:0.03});speed=Math.random()*0.5+0.2;startY=height+50;}else if(config.shape==='snow'){const s=Math.random()*3+2;p.moveTo(-s,0);p.lineTo(s,0);p.moveTo(0,-s);p.lineTo(0,s);p.moveTo(-s*0.7,-s*0.7);p.lineTo(s*0.7,s*0.7);p.moveTo(-s*0.7,s*0.7);p.lineTo(s*0.7,-s*0.7);p.stroke({color:color,width:1.5,alpha:Math.random()*0.5+0.5});speed=Math.random()*1.5+1.0;startY=-50;}else{p.circle(0,0,Math.random()*3+1);p.fill(color);}particles.push({sprite:p,x:startX,y:startY,baseX:startX,angle:Math.random()*Math.PI*2,speedY:speed,baseRadius:1,shape:config.shape||'dust'});this.app!.stage.addChild(p);}const scaleMultiplier=1+(avg/255)*(size/50);particles.forEach(p=>{if(p.shape==='snow'){p.y+=p.speedY;p.angle+=0.02;p.x=p.baseX+Math.sin(p.angle)*30;if(p.y>height+20){p.y=-20;p.baseX=Math.random()*width;}p.sprite.scale.set(1+(avg/255)*0.5);}else if(p.shape==='smoke'){p.y-=p.speedY;if(p.y<-100){p.y=height+100;p.x=Math.random()*width;}p.sprite.scale.set(1+(avg/255)*(size/20));}else{p.y-=p.speedY+(avg/255)*2;if(p.y<-10){p.y=height+10;p.x=Math.random()*width;}p.sprite.scale.set(scaleMultiplier);p.sprite.alpha=config.glow?Math.min(1,0.4+(avg/255)):0.6;}p.sprite.x=p.x;p.sprite.y=p.y;});});}public setVisible(visible:boolean){if(this.app&&this.app.canvas){this.app.canvas.style.display=visible?'block':'none';}}public resize(width:number,height:number){if(this.app&&this.app.renderer){this.app.renderer.resize(width,height);}}}